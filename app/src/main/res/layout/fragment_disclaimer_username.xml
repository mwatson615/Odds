<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primaryColor"
    android:id="@+id/fragmentDisclaimer"
    android:orientation="vertical">

        <TextView
            android:id="@+id/disclaimerTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginStart="20dp"
            android:layout_marginBottom="10dp"
            android:text="@string/disclaimer"
            android:textAlignment="center"
            android:textColor="@color/primaryTextColor"
            android:textSize="18sp"
            android:fontFamily="@font/averia_sans_libre_light"/>

    <ProgressBar
        android:id="@+id/usernameSearchProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <androidx.appcompat.widget.SearchView
        android:id="@+id/disclaimerSearchView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="20dp"
        android:theme="@style/SearchViewDark"
        app:searchIcon="@drawable/ic_search_black_24dp"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:maxLines="1"
        app:iconifiedByDefault="false"
        app:queryHint="Choose a Username">

    </androidx.appcompat.widget.SearchView>

    <Button
        android:id="@+id/saveUsernameButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="@color/primaryColor"
        style="@style/Button"
        android:foreground="?android:attr/selectableItemBackground"
        android:layout_marginTop="10dp"
        android:elevation="4dp"
        android:text="@string/save_and_continue"/>

</LinearLayout>
